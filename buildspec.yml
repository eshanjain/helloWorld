version: 0.2

phases:
  build:
    commands:
      - df -h 
      - ls -lah  /var/codebuild/reserve/
      - echo "Filling disk space until 10MB remains..."
      - df -h 
      - ls -lah  /var/codebuild/reserve
      - hostname
